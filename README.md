This is the comp2171 repo
Data is grouped based on function
Data folder is for database activities
Payment has interfaces and classes for processing payment
Basic Data is all basic classics that handle data: Customer, Device, Job,
Transaction,
GUI folder only handles gui buttons etc
Job folder is for classes handling job creation

Database is in sqlite: erms.db file
To do:
- adding customer to db Customer table when save is clicked
- view customer should read data from Customer table in erms.db and list in upper panel
- view Job should read jobs in Job table and list in Upper panel
- when you right click a context menu should appear that list options 
based on implementation of GuiFuncProcessor.java for both view Jobs and customers
- Generate report should provide a menu pull data from erms.db based on filters
